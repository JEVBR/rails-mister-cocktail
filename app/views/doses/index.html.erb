<!--index.html.erb -->
<H1>DOSE</H1>
<% @dose.each do |dose| %>
  <H2>

    <%= link_to dose.name, dose_path(dose) %>
  </H2>
  <p>
    <%= link_to "remove dose", dose_path(dose), method: :delete %>
    <%= link_to "add dose", new_dose_path, method: :get %>
</p>

<% end %>
